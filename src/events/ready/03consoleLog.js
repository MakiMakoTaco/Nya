// module.exports = async (client) => {
// 	const logChannel = await client.channels.fetch(process.env.LOG_CHANNEL_ID);

// 	console.log(`${client.user.tag} is online.`);
// 	logChannel.send(`${client.user.username} is online.`);
// };
